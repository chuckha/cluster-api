apiVersion: apps/v1
kind: StatefulSet
metadata:
  name: controller-manager
  namespace: system
spec:
  template:
    spec:
      containers:
      - image: gcr.io/kubernetes1-226021/cluster-api-controller-amd64:dev
        name: manager